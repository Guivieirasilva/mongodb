<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redirection</title>
</head>
<body>
    <h1>Edite um Link</h1>

    <form action="/edit/<%= body.id%>" method="POST">
        <input type="text" name="title" placeholder="Title" class="field" value="<%= body.title%> " >

        <input type="text" name="description" placeholder="Description" class="field" value="<%= body.description%>">

        <input type="text" name="url" placeholder="URL" class="field" value="<%= body.url%>">

        <button>Salvar</button>
    </form>

    <% if(error){ %> 
    <h3><%= error.message %> </h3>
        <% } %> 
</body>
</html>